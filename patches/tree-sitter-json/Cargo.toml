[package]
name = "tree-sitter-json"
version = "0.24.8"
edition = "2021"
license = "MIT"
# Patched: grammar C sources pre-compiled to lib/libtree_sitter_json.a for wasm32-wasip2
# (zig cc, see docs/WASM_BUILD_PATCHES.md). Native builds compile the C sources.
build = "build.rs"
autolib = false

[lib]
name = "tree_sitter_json"
path = "bindings/rust/lib.rs"

[dependencies.tree-sitter-language]
version = "0.1"

[build-dependencies]
cc = "1"
